import { module, skip } from 'qunit';
import { setupRenderingTest } from 'dummy/tests/helpers';

module('Integration | Component | dashboard/calendar-filters', function (hooks) {
  setupRenderingTest(hooks);

  skip('it renders', async function () {
    /*
    Filters are well tested in acceptance tests
    Leaving this file here in case we have future need
    for some specific integration tests on this component
    */
  });
});
