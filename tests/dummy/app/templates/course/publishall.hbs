<LinkTo @route="course" @model={{this.model}}>
  {{t "general.backToTitle" title=this.model.title}}
</LinkTo>

<PublishAllSessions
  @sessions={{this.model.sessions}}
  @saved={{fn this.returnToList}}
/>
