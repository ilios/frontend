<CourseSessions
  @course={{this.model}}
  @canCreateSession={{this.canCreateSession}}
  @canUpdateCourse={{this.canUpdateCourse}}
  @sortBy={{this.sortSessionsBy}}
  @setSortBy={{fn (mut this.sortSessionsBy)}}
  @filterBy={{this.filterSessionsBy}}
  @setFilterBy={{fn (mut this.filterSessionsBy)}}
/>
