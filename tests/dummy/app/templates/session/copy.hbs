<div class="session-details">
  {{session-overview
    session=this.model
    editable=this.canUpdate
    showCheckLink=false
    sessionTypes=this.model.sessionTypes
  }}
  {{session-copy session=this.model visit=(action "loadSession")}}
</div>
