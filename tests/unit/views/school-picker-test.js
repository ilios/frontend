import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:school-picker', 'SchoolPickerView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
