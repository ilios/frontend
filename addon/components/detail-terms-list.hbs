<div
  class="detail-terms-list"
  {{did-insert (perform this.load) @terms}}
  {{did-update (perform this.load) @terms}}
>
  {{#if (is-array this.sortedTerms)}}
    <div data-test-title>
      {{! template-lint-disable no-invalid-interactive }}
      <strong
        class={{if @manage "link-button"}}
        role={{if @canEdit "button"}}
        {{on "click" (if @manage @manage (noop))}}
      >
        {{@vocabulary.title}}
      </strong> ({{@vocabulary.school.title}})
      {{#if (and @canEdit (not @vocabulary.active))}}
        <span class="inactive">
          ({{t "general.inactive"}})
        </span>
      {{/if}}
    </div>
    <ul class="{{if @canEdit "removable-list"}} selected-taxonomy-terms">
      {{#each this.sortedTerms as |term|}}
        {{#if @canEdit}}
          <DetailTermsListItem
            @canEdit={{true}}
            @remove={{@remove}}
            @term={{term}}
          />
        {{else}}
          <DetailTermsListItem @canEdit={{false}} @term={{term}} />
        {{/if}}
      {{/each}}
    </ul>
  {{/if}}
</div>
