import Ember from 'ember';
import layout from '../templates/components/dashboard-view-picker';

const { Component } = Ember;

export default Component.extend({
  layout,
  classNames: ['dashboard-view-picker'],
});
