<section
  class="course-visualize-objectives"
  data-test-course-visualize-objectives
  {{did-insert (perform this.load)}}
>
  {{#unless this.load.isRunning}}
    <div class="backtolink">
      <LinkTo @route="course-visualizations" @model={{@model}}>
        {{t "general.otherVisualizations"}}
      </LinkTo>
    </div>
    <h2 class="clickable" data-test-title>
      <LinkTo @route="course" @model={{@model}}>
        {{@model.title}}
        {{#if this.academicYearCrossesCalendarYearBoundaries}}
          {{@model.year}} - {{add @model.year 1}}
        {{else}}
          {{@model.year}}
        {{/if}}
      </LinkTo>
    </h2>
    <h3>
      {{t "general.objectivesTotalInstructionalTime"}}
    </h3>
    <VisualizerCourseObjectives @course={{@model}} />
  {{/unless}}
</section>
