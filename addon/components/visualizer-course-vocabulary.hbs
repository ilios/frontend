<div
  class="visualizer-course-vocabulary {{unless @isIcon "not-icon"}}"
  {{did-insert (perform this.load) @course @vocabulary}}
  {{did-update (perform this.load) @course @vocabulary}}
>
  {{#unless this.load.isRunning}}
    {{#if (or @isIcon this.data.length)}}
      <SimpleChart
        @name="horz-bar"
        @isIcon={{@isIcon}}
        @data={{this.data}}
        @click={{this.barClick}}
        @hover={{perform this.barHover}}
        @leave={{perform this.barHover}} as |chart|
      >
        {{#if this.tooltipContent}}
          <chart.tooltip @title={{this.tooltipTitle}}>
            {{this.tooltipContent}}
          </chart.tooltip>
        {{/if}}
      </SimpleChart>
    {{/if}}
  {{/unless}}
</div>
