<th
  class="clickable sortable text-{{this.align}} {{if this.hideFromSmallScreen "hide-from-small-screen"}}"
  colspan={{this.colspan}}
  role="button"
  title={{this.title}}
  {{on "click" this.click}}
>
  {{yield}}<FaIcon @icon={{this.sortIcon}} />
</th>

