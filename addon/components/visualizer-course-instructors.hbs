<div
  class="visualizer-course-instructors {{if (not @isIcon) "not-icon"}}"
  {{did-insert (perform this.load) @course}}
  {{did-update (perform this.load) @course}}
>
  {{#unless this.load.isRunning}}
    {{#if (or @isIcon this.data.length)}}
      <SimpleChart
        @name={{this.chartType}}
        @isIcon={{@isIcon}}
        @data={{this.filteredData}}
        @click={{action "barClick"}}
        @hover={{perform this.barHover}}
        @leave={{perform this.barHover}} as |chart|
      >
        {{#if this.tooltipContent}}
          <chart.tooltip @title={{this.tooltipTitle}}>
            {{this.tooltipContent}}
          </chart.tooltip>
        {{/if}}
      </SimpleChart>
    {{/if}}
  {{/unless}}
</div>
