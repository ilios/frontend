<div
  class="visualizer-course-term {{if (not @isIcon) "not-icon"}}"
  {{did-insert (perform this.load) @course @term}}
  {{did-update (perform this.load) @course @term}}
>
  {{#unless this.load.isRunning}}
    {{#if (or true @isIcon this.data.length)}}
      <SimpleChart
        @name="horz-bar"
        @isIcon={{@isIcon}}
        @data={{this.data}}
        @hover={{perform this.barHover}}
        @leave={{perform this.barHover}} as |chart|
      >
        {{#if this.tooltipContent}}
          <chart.tooltip @title={{this.tooltipTitle}}>
            {{this.tooltipContent}}
          </chart.tooltip>
        {{/if}}
      </SimpleChart>
    {{/if}}
  {{/unless}}
</div>
