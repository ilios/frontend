<span class="status publication-status {{this.publicationStatus}}" data-test-publication-status>
  {{#if @showIcon}}
    <FaIcon @icon={{this.iconKey}} @title={{t this.textKey}} />
  {{/if}}
  {{#if @showText}}
    <span data-test-text>
      {{t this.textKey}}
    </span>
  {{/if}}
</span>
