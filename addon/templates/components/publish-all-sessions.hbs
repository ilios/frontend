{{#liquid-if this.isSaving class="crossFade"}}
  <WaitSaving
    @showProgress={{true}}
    @totalProgress={{this.totalSessionsToSave}}
    @currentProgress={{this.currentSessionsSaved}}
  />
{{/liquid-if}}
<section class="publish-all-sessions-unpublishable">
  <div
    class="title {{if this.unPublishableCollapsed "collapsed" "collapsible"}}"
    role="button"
    {{on "click" (action "toggleUnPublishableCollapsed")}}
  >
    {{t "general.incompleteSessions"}} ({{get (await this.unPublishableSessions) "length"}})
  </div>
  {{#unless this.unPublishableCollapsed}}
    <div class="content">
      <table>
        <thead>
          <tr>
            <th>
              {{t "general.sessionTitle"}}
            </th>
            <th>
              {{t "general.offerings"}}
            </th>
            <th>
              {{t "general.terms"}}
            </th>
            <th>
              {{t "general.objectives"}}
            </th>
            <th>
              {{t "general.meshTerms"}}
            </th>
          </tr>
        </thead>
        <tbody>
          {{#each (await this.unPublishableSessions) as |session|}}
            <tr>
              <td>
                {{session.title}}
              </td>
              {{#if session.offerings.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.offerings.length}})
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
              {{#if session.terms.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.terms.length}})
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
              {{#if session.objectives.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.objectives.length}})
                  {{#if session.showUnlinkIcon}}
                    <FaIcon
                      @icon="unlink"
                      class="clickable"
                      @click={{action "transitionToSession" session}}
                    />
                  {{/if}}
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
              {{#if session.meshDescriptors.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.meshDescriptors.length}})
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  {{/unless}}
</section>
<section class="publish-all-sessions-publishable">
  <div
    class="title {{if this.publishableCollapsed "collapsed" "collapsible"}}"
    role="button"
    {{on "click" (action "togglePublishableCollapsed")}}
  >
    {{t "general.completeSessions"}} ({{get (await this.publishableSessions) "length"}})
  </div>
  {{#unless this.publishableCollapsed}}
    <div class="content">
      <table>
        <thead>
          <tr>
            <th>
              {{t "general.sessionTitle"}}
            </th>
            <th>
              {{t "general.offerings"}}
            </th>
            <th>
              {{t "general.terms"}}
            </th>
            <th>
              {{t "general.objectives"}}
            </th>
            <th>
              {{t "general.meshTerms"}}
            </th>
          </tr>
        </thead>
        <tbody>
          {{#each (await this.publishableSessions) as |session|}}
            <tr>
              <td>
                {{session.title}}
              </td>
              {{#if session.offerings.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.offerings.length}})
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
              {{#if session.terms.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.terms.length}})
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
              {{#if session.objectives.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.objectives.length}})
                  {{#if session.showUnlinkIcon}}
                    <FaIcon
                      @icon="unlink"
                      class="clickable"
                      @click={{action "transitionToSession" session}}
                    />
                  {{/if}}
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
              {{#if session.meshDescriptors.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.meshDescriptors.length}})
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  {{/unless}}
</section>
<section class="publish-all-sessions-overridable">
  <div class="title">
    {{t "general.reviewSessions"}} ({{get (await this.overridableSessions) "length"}})
  </div>
  <div class="content">
    {{#if (get (await this.overridableSessions) "length")}}
      <button disabled={{await this.allSessionsAsIs}} {{on "click" (action "publishAllAsIs")}}>
        {{t "general.publishAsIs"}}
      </button>
      <button disabled={{this.noSessionsAsIs}} {{on "click" (action "publishNoneAsIs")}}>
        {{t "general.markAsScheduled"}}
      </button>
      <table>
        <thead>
          <tr>
            <th>
              {{t "general.actions"}}
            </th>
            <th>
              {{t "general.sessionTitle"}}
            </th>
            <th>
              {{t "general.offerings"}}
            </th>
            <th>
              {{t "general.terms"}}
            </th>
            <th>
              {{t "general.objectives"}}
            </th>
            <th>
              {{t "general.meshTerms"}}
            </th>
          </tr>
        </thead>
        <tbody>
          {{#each (await this.overridableSessions) as |session|}}
            <tr>
              <td>
                <ul>
                  <li>
                    <input
                      type="checkbox"
                      checked={{is-in this.sessionsToOverride session}}
                      {{on "click" (fn (action "toggleSession") session)}}
                    >
                    <span
                      class="clickable"
                      role="button"
                      {{on "click" (fn (action "toggleSession") session)}}
                    >
                      {{t "general.publishAsIs"}}
                    </span>
                  </li>
                  <li>
                    <input
                      type="checkbox"
                      checked={{not-in this.sessionsToOverride session}}
                      {{on "click" (fn (action "toggleSession") session)}}
                    >
                    <span
                      class="clickable"
                      role="button"
                      {{on "click" (fn (action "toggleSession") session)}}
                    >
                      {{t "general.markAsScheduled"}}
                    </span>
                  </li>
                </ul>
              </td>
              <td>
                {{session.title}}
              </td>
              {{#if session.offerings.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.offerings.length}})
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
              {{#if session.terms.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.terms.length}})
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
              {{#if session.objectives.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.objectives.length}})
                  {{#if session.showUnlinkIcon}}
                    <FaIcon
                      @icon="unlink"
                      class="clickable"
                      @click={{action "transitionToSession" session}}
                    />
                  {{/if}}
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
              {{#if session.meshDescriptors.length}}
                <td class="yes">
                  {{t "general.yes"}} ({{session.meshDescriptors.length}})
                </td>
              {{else}}
                <td class="no">
                  {{t "general.no"}}
                </td>
              {{/if}}
            </tr>
          {{/each}}
        </tbody>
      </table>
    {{/if}}
  </div>
</section>
<div class="publish-all-sessions-review">
  {{#if (await this.showWarning)}}
    <span class="unlinked-warning" data-test-unlinked-warning>
      {{t "general.unlinkedObjectives"}}
    </span>
    <FaIcon
      @icon="unlink"
      class="clickable"
      @click={{action "transitionToCourse"}}
    />
    <FaIcon
      @icon="chart-bar"
      class="clickable"
      @click={{action "transitionToVisualizeObjectives"}}
    />
  {{/if}}
  <p>
    {{t
      "general.publishAllConfirmation"
      publishCount=(await this.publishCount)
      scheduleCount=(await this.scheduleCount)
      ignoreCount=(await this.ignoreCount)
    }}
  </p>
  <button {{on "click" (action "save")}}>
    {{t "general.go"}}
  </button>
</div>
