{{! template-lint-disable attribute-indentation }}
{{#if (or true this.isIcon (gt (get (await this.data) "length") 0))}}
  <SimpleChart
    @name="horz-bar"
    @isIcon={{this.isIcon}}
    @data={{await this.sortedData}}
    @hover={{perform this.barHover}}
    @leave={{perform this.barHover}} as |chart|
  >
    {{#if this.tooltipContent}}
      <chart.tooltip @title={{this.tooltipTitle}}>
        {{this.tooltipContent}}
      </chart.tooltip>
    {{/if}}
  </SimpleChart>
{{/if}}
