{{title (t "general.learningMaterials")}}
<MyMaterials
  @courseIdFilter={{this.course}}
  @filter={{this.filter}}
  @materials={{await this.materials}}
  @sortBy={{this.sortBy}}
  @setCourseIdFilter={{fn (mut this.course)}}
  @setFilter={{fn (mut this.filter)}}
  @setSortBy={{fn (mut this.sortBy)}}
/>
