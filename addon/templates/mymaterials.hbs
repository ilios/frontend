{{title (t "general.learningMaterials")}}
<MyMaterials
  @courseIdFilter={{this.course}}
  @filter={{this.filter}}
  @materials={{this.materials}}
  @sortBy={{this.sortBy}}
  @setCourseIdFilter={{action (mut this.course)}}
  @setFilter={{action (mut this.filter)}}
  @setSortBy={{action (mut this.sortBy)}}
/>
