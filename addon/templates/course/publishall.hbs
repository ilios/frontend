<LinkTo @route="course" @model={{@model}}>
  {{t "general.backToTitle" title=@model.title}}
</LinkTo>
<PublishAllSessions
  @course={{@model}}
  @sessions={{await @model.sessions}}
  @saved={{this.returnToList}}
/>
