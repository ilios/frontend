<span
  class="{{unless @isIcon "not-icon"}} visualizer-session-type-terms"
  data-test-visualizer-session-type-terms
  ...attributes
>
  {{#if this.isLoaded}}
    {{#if (or @isIcon (gt this.data.length 0))}}
        <SimpleChart
          @name="donut"
          @isIcon={{@isIcon}}
          @data={{this.data}}
          @hover={{perform this.donutHover}}
          @leave={{perform this.donutHover}} as |chart|
        >
          {{#if this.tooltipContent}}
            <chart.tooltip @title={{this.tooltipTitle}}>
              <h5>
                {{t "general.courses"}} ({{this.tooltipContent.coursesCount}})
              </h5>
              <p>
                {{this.tooltipContent.courses}}
              </p>
              <h5>
                {{t "general.sessions"}} ({{this.tooltipContent.sessionsCount}})
              </h5>
              <p>
                {{this.tooltipContent.sessions}}
              </p>
            </chart.tooltip>
          {{/if}}
        </SimpleChart>
      {{else}}
        <div class="with-hours">
          <h4>
            <FaIcon @icon="face-meh" class="meh-o" />
          </h4>
        </div>
      {{/if}}
  {{/if}}
</span>
