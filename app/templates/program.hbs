<fieldset>
  {{#if isEditing}}
    <legend>Editing {{title}}</legend>
    <label {{bindAttr for="view.titleField.elementId"}}>Title</label>
    {{view Ember.TextField value=title viewName="titleField"}}

    <label {{bindAttr for="view.shortTitleField.elementId"}}>Short Title</label>
    {{view Ember.TextField value=shortTitle viewName="shortTitleField"}}

    <label {{bindAttr for="view.durationField.elementId"}}>Duration in years</label>
    {{view Ember.Select content=durationOptions value=duration viewName="durationField"}}

    <button {{action 'save'}}>Save</button>

  {{else}}
      <legend>{{title}} Program</legend>
      <p><strong>Title</strong>: {{title}}</p>
      <p><strong>Short Title</strong>: {{shortTitle}}</p>
      <p><strong>Duration</strong>: {{duration}} years</p>
      <button {{action 'edit'}}>Edit</button>
  {{/if}}
</fieldset>
<h2>Program Years:</h2>
<table class="grid-table">
  <thead>
    <tr>
      <th>Academic Year</th>
      <th>Competencies</th>
      <th>Objectives</th>
      <th>Directors</th>
      <th>Published</th>
    </tr>
  </thead>
  <tbody>
    {{#each programYears}}
      <tr>
        <td>{{#link-to 'program-year' id}}{{academicYear}}{{/link-to}}</td>
        <td>{{competencies.length}}</td>
        <td>{{objectives.length}}</td>
        <td>{{directors.length}}</td>
        <td>{{nicePublished}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>
<!-- <button {{action 'addNew'}}>Add New Program</button> -->
