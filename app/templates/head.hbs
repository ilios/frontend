<title>
  {{t 'general.ilios'}}
  {{#if model.titleTokens.length}}
    {{#each model.titleTokens as |token|}}
      {{t token}}&nbsp;
    {{/each}}
  {{/if}}
</title>
<link rel="stylesheet" href="/school-theming/css/school.css"> 
