{{#each terms as |term|}}
  {{#if (is-in selectedTerms term)}}
    <li><div class="selected" {{action 'remove' term}}>{{term.title}} <span class="actions">{{fa-icon 'remove'}}</span></div>
    {{#if term.hasChildren}}
      {{selectable-terms-list selectedTerms=selectedTerms terms=term.children add='add' remove='remove'}}
    {{/if}}
    </li>
  {{else}}
    <li><div {{action 'add' term}}>{{term.title}} <span class="actions">{{fa-icon 'plus'}}</span></div>
      {{#if term.hasChildren}}
        {{selectable-terms-list selectedTerms=selectedTerms terms=term.children add='add' remove='remove'}}
      {{/if}}
    </li>
  {{/if}}
{{/each}}
