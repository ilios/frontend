{{component chartName
  data=data
  width=width
  height=height
  icon=icon
  hover=(action 'hover')
  leave=(action 'leave')
}}

{{yield (hash
  tooltip=(component 'ilios-chart-tooltip' slice=tooltipSlice location=tooltipLocation)
)}}
