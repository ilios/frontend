{{component chartName
  data=data
  width=width
  height=height
  hover=(action 'hover')
  leave=(action 'leave')
}}

{{yield (hash
  tooltip=(component 'ilios-chart-tooltip' slice=tooltipSlice location=tooltipLocation)
)}}
