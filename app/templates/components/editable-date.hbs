<span class='content'>
  {{#unless isEditing}}
    <span {{action 'edit'}} class='editable'>{{moment date format}}</span>
  {{else}}
  <span class='editor'>
    <span class='control'>
      {{pikaday-input value=date format="MM/DD/YYYY"}}
      {{fa-icon 'calendar'}}
    </span>
    <span class='actions'>
      <button class='save' {{action 'save'}}>{{fa-icon 'check' classNames='add'}}</button>
      <button class='cancel' {{action 'cancel'}}>{{fa-icon 'undo' classNames='remove'}}</button>
    </span>
  </span>
  {{/unless}}
</span>
