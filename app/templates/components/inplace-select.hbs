{{#unless isEditing}}
  <span {{action 'edit'}} class='editable'>{{displayValue}}</span>
{{else}}
  <span class='editor'>
    <span class='control'>
      {{#x-select value=workingValue action="changeSelection"}}
        {{#if showSelectPrompt}}
          {{#x-option value=null}}{{selectPrompt}}{{/x-option}}
        {{/if}}
        {{#each option in proxiedOptions}}
          {{#x-option value=option.value}}{{option.label}}{{/x-option}}
        {{/each}}
      {{/x-select}}
    </span>
    <span class='actions'>
      <button class='save' {{action 'save'}}>{{fa-icon 'check' classNames='add'}}</button>
      <button class='cancel' {{action 'cancel'}}>{{fa-icon 'undo' classNames='remove'}}</button>
    </span>
  </span>
{{/unless}}
