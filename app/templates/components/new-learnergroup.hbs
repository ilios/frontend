<section class='newlearnergroup'>
  <div class="newlearnergroup-title">{{t 'learnerGroups.new'}}</div>
  <div class="detail-content">
    <label class="inline-label-data-block">
      {{t 'general.title'}}
      {{input value=learnerGroup.title placeholder=placeholder}}
    </label>
    <button class='cancel text' {{action 'cancel'}}>{{t 'general.cancel'}}</button>
    <button class='done text' {{action 'save'}}>{{t 'general.done'}}</button>
</div>
</section>
