{{! template-lint-disable no-unused-block-params }}
{{#each (repeat @count) as |empty|}}
  <div>
    <span colspan=1></span>
    <span colspan=8>{{truncate (repeat (random 3 15) 'ilios rocks') 100}}</span>
    <span colspan=6>{{repeat (random 1 3) 'loading '}}</span>
    <span colspan=2>{{random 1 99}}</span>
    <span colspan=2>{{random 1 99}}</span>
    <span colspan=2>{{random 1 99}}</span>
    <span colspan=4>{{moment '2005-06-24' 'L LT'}}</span>
    <span colspan=2>{{random 1 99}}</span>
    <span colspan=2>{{fa-icon 'star-half-full'}}</span>
  </div>
{{/each}}
