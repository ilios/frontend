{{#if term.description.length}}
  {{#tooltip-on-component}}
    {{term.description}}
  {{/tooltip-on-component}}
{{/if}}
{{term.title}}
<span class="actions">
  {{#if isSelected}}
    {{fa-icon 'remove'}}
  {{else if term.active}}
    {{fa-icon 'plus' class='add'}}
  {{/if}}
</span>
