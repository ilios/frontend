<h1>
  {{course.title}}
</h1>
{{#if (is-fulfilled objectiveData)}}
  {{chart-bar data=(await objectiveData) size=size padding=padding}}
  {{chart-pie data=(await objectiveData) size=size padding=padding}}
{{else}}
  {{fa-icon 'spinner' spin=true}}
{{/if}}
