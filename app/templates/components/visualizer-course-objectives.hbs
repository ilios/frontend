<h1>
  {{course.title}}
</h1>
{{#if (is-fulfilled objectiveData)}}
  {{chart-pie data=(await objectiveData) width=width height=height}}
  {{chart-donut data=(await objectiveData) width=width height=height}}
{{else}}
  {{fa-icon 'spinner' spin=true}}
{{/if}}
