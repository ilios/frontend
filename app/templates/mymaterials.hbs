{{title (t "general.learningMaterials")}}
{{my-materials
  materials=model
  sortBy=sortBy
  setSortBy=(action (mut sortBy))
  filter=filter
  setFilter=(action (mut filter))
  courseIdFilter=course
  setCourseIdFilter=(action (mut course))
}}
