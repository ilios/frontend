<li {{bind-attr class="selected:disabled:enabled"}}>
  {{#unless selected}}
    <span {{translateAttr title="programs.programYear.addTopic"}} class='add' {{action "add" this}}>{{fa-icon "plus-circle"}}</span>
  {{else}}
    {{fa-icon "circle"}}
  {{/unless}}
  {{title}}
</li>
