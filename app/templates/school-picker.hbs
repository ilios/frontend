{{#if currentUser.canChangeSchool}}
  {{view 'select'
    optionLabelPath="content.title"
    content=currentUser.schools
    value=currentUser.currentSchool
    selection=selectedSchool
  }}
{{/if}}
