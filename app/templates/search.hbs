<GlobalSearch
  @page={{this.page}}
  @query={{this.query}}
  @onQuery={{action (mut this.query)}}
  @onNextPage={{action "nextPage"}}
  @onPrevPage={{action "prevPage"}}
  @onSelectPage={{action (mut this.page)}} />
