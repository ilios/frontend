{{learnergroup-summary
  setIsEditing=(action (mut isEditing))
  canCreate=canCreate
  canUpdate=canUpdate
  canDelete=canDelete
  setIsBulkAssigning=(action (mut isBulkAssigning))
  sortUsersBy=sortUsersBy
  setSortUsersBy=(action (mut sortUsersBy))
  learnerGroup=model
  isEditing=isEditing
  isBulkAssigning=isBulkAssigning
}}
