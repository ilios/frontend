{{title (t "general.learnerGroups")}}
<LearnergroupSummary
  @setIsEditing={{action (mut this.isEditing)}}
  @canCreate={{this.canCreate}}
  @canUpdate={{this.canUpdate}}
  @canDelete={{this.canDelete}}
  @setIsBulkAssigning={{action (mut this.isBulkAssigning)}}
  @sortUsersBy={{this.sortUsersBy}}
  @setSortUsersBy={{action (mut this.sortUsersBy)}}
  @learnerGroup={{@model}}
  @isEditing={{this.isEditing}}
  @isBulkAssigning={{this.isBulkAssigning}}
/>
