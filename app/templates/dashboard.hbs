<section class='dashboard fullwidth'>
  <div class='calendar-profile'>
    {{t 'dashboard.myCalendar'}}

    {{toggle-wide
      value=showCalendar
      onLabel='offerings.calendarOn'
      offLabel='offerings.calendarOff'
      action='toggleShowCalendar'
      cssId='calendar-profile'
    }}
  </div>
  {{#liquid-if showCalendar class='vertical'}}
    {{dashboard-calendar
      selectedDate=selectedDate
      selectedView=selectedView
      mySchedule=mySchedule
      showFilters=showFilters
      changeDate='changeDate'
      changeView='changeView'
      selectEvent='selectEvent'
      toggleMySchedule='toggleMySchedule'
      toggleShowFilters='toggleShowFilters'
      academicYearSelectedByUser=academicYearSelectedByUser
      schoolPickedByUser=schoolPickedByUser
      changeAcademicYear='changeAcademicYear'
      changeSchool='changeSchool'
      courseFilters=courseFilters
      toggleCourseFilters='toggleCourseFilters'
    }}
  {{else}}
    {{dashboard-agenda}}
  {{/liquid-if}}
  {{dashboard-myreports}}
  {{dashboard-mycourses}}
</section>
