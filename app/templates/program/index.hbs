<ProgramyearList
  @programYears={{await @model.programYears}}
  @canCreate={{this.canCreate}}
  @program={{@model}}
  @lock={{action "lockProgramYear"}}
  @unlock={{action "unlockProgramYear"}}
  @activate={{action "activateProgramYear"}}
/>
