<section id='instructor-groups' class='fullwidth'>
  {{#if model}}
    {{input type='text' value=filter placeholder='Filter by title'}}
    {{#each group in filteredContent}}
      {{ilios-instructorgroup group=group}}
    {{/each}}
  {{else}}
    <p>{{t 'instructorGroups.noGroups'}}</p>
  {{/if}}
</section>
<section class='new fullwidth'>
  <button {{action 'createNewGroup'}}>{{t 'instructorGroups.new'}}</button>
</section>
