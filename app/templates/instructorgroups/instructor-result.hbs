<li {{bind-attr class="selected:disabled:enabled"}}>
  {{#unless selected}}
    <span {{translateAttr title="instructorGroups.add"}} class='add' {{action "addInstructor" this}}>{{fa-icon "plus-circle"}}</span>
  {{else}}
    {{fa-icon "circle"}}
  {{/unless}}
  {{fullName}} {{email}}
</li>
