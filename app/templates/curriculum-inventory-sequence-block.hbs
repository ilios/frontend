{{page-title (t "general.curriculumInventoryReports")}}
<CurriculumInventory::SequenceBlockDetails
  @sequenceBlock={{@model}}
  @canUpdate={{this.canUpdate}}
  @sortSessionsBy={{this.sortSessionsBy}}
  @setSortSessionBy={{action (mut this.sortSessionsBy)}}
/>
<CurriculumInventory::SequenceBlockList
  @report={{await @model.report}}
  @parent={{@model}}
  @canUpdate={{this.canUpdate}}
  @sequenceBlocks={{await @model.children}}
  @remove={{action "removeChildSequenceBlock"}}
/>
