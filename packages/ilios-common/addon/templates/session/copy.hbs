<div class="session-details">
  <Session::Overview
    @session={{@model}}
    @editable={{this.canUpdate}}
    @showCheckLink={{false}}
    @sessionTypes={{@model.sessionTypes}}
  />
  <SessionCopy @session={{@model}} @visit={{this.loadSession}} />
</div>