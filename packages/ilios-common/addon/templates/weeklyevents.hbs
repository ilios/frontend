{{#if this.showBackLink}}
  <BackLink />
{{/if}}
<WeeklyEvents
  @year={{this.year}}
  @expandedWeeks={{this.expandedWeeks}}
  @weekInFocus={{this.week}}
  @setYear={{set this "year"}}
  @toggleOpenWeek={{this.toggleOpenWeek}}
/>