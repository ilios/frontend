import Controller from '@ember/controller';
import { tracked } from '@glimmer/tracking';

export default class SessionTypeVisualizeVocabulariesController extends Controller {
  @tracked dataLoaded = false;
}
